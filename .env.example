# --- Servidor API + SQLite (npm run dev / Docker runtime) ---
DATA_DIR=.data
# PORT=8080
# SQLITE_PATH=   # default: $DATA_DIR/overwatch.db

# --- Cliente: API del mismo host (prod suele ir vacío) ---
# VITE_API_BASE=
